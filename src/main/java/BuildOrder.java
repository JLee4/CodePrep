public class BuildOrder {
    /*
    You are given a list of projects and a list of dependencies (which is a list of pairs of projects, where
    the second project is dependent on the first project). All of a project's dependencies must be build before the
    project is. Find a build that will allow the projects to be build.
    If there is no valid build order, return an error.
     */
}
