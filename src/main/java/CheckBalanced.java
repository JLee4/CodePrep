import objects.BinaryTreeNode;

public class CheckBalanced {
    /*
    Implement a function to check if a binary tree is balanced. A balance in this question is a tree such that the
    heights of the two subtrees of any node never differs more than one.
     */
    public static boolean isBalanced(BinaryTreeNode root) {

    }
}
