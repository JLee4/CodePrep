public class StackMin {
    /*
    Design a stack which, in addition to pop and push, has a function, min, that returns the minimum element.
    Push, pop, and min should all return in O(1).
     */
}
